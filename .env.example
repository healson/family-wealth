# 部署环境变量示例：复制为 .env 后按需修改
# 若使用 .env，docker-compose.yml 中需通过 ${VAR} 引用

# 初始管理员密码（首次登录后请在应用内修改）
ADMIN_PASSWORD=admin123

# 是否写入演示数据：true/false
SEED_DEMO_DATA=true

# JWT 签名密钥（生产环境请改为随机长字符串，例如 openssl rand -hex 32）
JWT_SECRET=please-change-me

# 登录有效期（小时）
JWT_EXPIRE_HOURS=168

# 时区
TZ=Asia/Shanghai
